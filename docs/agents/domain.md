# Domain documentation

This is a single-context repository.

- Read `CONTEXT.md` at the repository root when it exists.
- Read architectural decisions from `docs/adr/` when that directory exists.
- Do not invent missing domain terminology or architectural decisions; infer only
  from the code and explicitly label those inferences.
